import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vehicle.maintenance.app',
  appName: 'Vehicle Maintenance App',
  webDir: 'dist'
};

export default config;
